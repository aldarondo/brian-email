{
  "name": "brian-email",
  "version": "1.0.0",
  "description": "MCP server that sends outbound email via a Gmail account — deployed on Synology NAS.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "serve": "node src/serve.js",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js tests/"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^4.21.0",
    "googleapis": "^144.0.0",
    "nodemailer": "^8.0.5"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "jest": "^30.3.0"
  }
}
